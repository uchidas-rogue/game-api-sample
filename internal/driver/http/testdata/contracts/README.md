# API レスポンス契約

このディレクトリの JSON はレスポンス**構造**の正本。値に意味は無く、型と入れ子の形だけが契約。

`internal/testutil/apicontract.AssertStructure` が各ハンドラのテストから参照する。
キー名の追加・削除・リネームをしたら、ここのファイルも同じ PR で更新すること
（このレスポンスを解釈する側 — k6 シナリオ等 — の変更が必要になるサイン）。
